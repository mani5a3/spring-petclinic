name: "Java_Maven_Gradle_Workflow"

on:
  workflow_dispatch:

jobs:
  reuse-java-worflow:
    uses: mani5a3/github_actions_poc/.github/workflows/java-maven-gradle-workflow.yaml@main
    with:
      java_version: "8"
      build_tool: "maven"
      maven_goals: "clean install"
      Sonar_ProjectKey: "gameoflife_github_actions"
      Sonar_ProjectName: "gameoflife_github_actions"
      Sonar_ProjectVersion: "1.0"
      runner: "windows"
# secrets: inherit pulls secrets from the calling workflow’s context and makes them available to the reusable workflow.
    secrets:
      Jfrog_Username: "admin"
      Jfrog_Password: "password"
      Sonar_Username: "admin"
      Sonar_Password: "admin"